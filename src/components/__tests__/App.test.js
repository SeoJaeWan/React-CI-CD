describe("테스트 실패", () => {
  it("실패한 테스트", () => {
    expect(true).toEqual(false);
  });
  it("실패한 테스트", () => {
    expect(true).toEqual(false);
  });
  it("실패한 테스트", () => {
    expect(true).toEqual(false);
  });
  it("실패한 테스트", () => {
    expect(true).toEqual(false);
  });
});
